apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: linkding-pvc-static
  namespace: linkding
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
