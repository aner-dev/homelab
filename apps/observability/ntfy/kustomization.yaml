apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability

resources:
  - ntfy-deployment.yaml
  - ntfy-svc.yaml

configMapGenerator:
  - name: server-config
    files:
      - server.yml
