apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - ocirepository.yaml
  - helmrelease.yaml
  - clusterissuer.yaml

labels:
  - app.kubernetes.io.name: "cert-manager"
  - networking.athanor.local/attached-gateway: "internal"
