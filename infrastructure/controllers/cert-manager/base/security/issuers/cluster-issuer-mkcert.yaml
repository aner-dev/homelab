apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: local-mkcert-ca-issuer
spec:
  ca:
    # This must match exactly the name of the secret you just encrypted
    secretName: mkcert-root-ca-secret
