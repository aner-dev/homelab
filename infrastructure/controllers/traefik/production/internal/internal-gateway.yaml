apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: internal-gateway
  namespace: traefik
  labels:
    networking.athanor.local/role: gateway
    networking.athanor.local/zone: internal
spec:
  gatewayClassName: traefik-internal
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              # Only namespaces with this label can attach to this Gateway
              networking.athanor.local/internal-gateway-access: "true"
    - name: websecure
      protocol: HTTPS
      port: 443
      allowedRoutes:
        namespaces:
          from: Selector
          selector:
            matchLabels:
              networking.athanor.local/internal-gateway-access: "true"

      tls:
        mode: Terminate
        certificateRefs:
          - name: athanor-wildcard-cert
